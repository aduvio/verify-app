# Project Verify

- Preserve this existing Flutter app and its approved screen arrangement. Pilot: Chalmette, never Gulfport.
- Intended primary recording device: one pair of Ray-Ban Meta glasses. Preserve the glasses-first workflow. The computer camera is a development test device and fallback, never a replacement; screen controls are the prototype/fallback.
- September 15, 2026: user confirmed glasses-native video with audible narration, imported and played on the paired iPhone. This is a hardware/native-app test only; Project Verify glasses integration remains unimplemented and untested. Physical webcam audio testing is paused by request, unresolved, not fixed. See docs/GLASSES_IOS_PREREQUISITES.md before integration work; manual imports are not the final glasses-first design.
- Development computer is Windows; user has no Mac access. Assess Codemagic hosted macOS builds and TestFlight installation instead of requiring a personal Mac. No account creation, billing, source/media upload, signing-key sharing or cloud build is authorized by the assessment alone.
- Ray-Ban Meta is not connected or supported by this build. Exact glasses model, paired phone, SDK compatibility and required capture/control capabilities must be verified before implementing a glasses adapter. Keep browser capture behind CameraService so inspection, storage, retakes and review can be reused.
- Capture targeted final-verification clips, not the entire oil change. Drain-plug and oil-filter clips require at least five seconds, including top-mounted filters.
- Under vehicle: drain plug, oil filter if accessible there, differential/axle area, engine underside, residual-oil cleanup confirmation.
- Under hood: top-mounted filter if applicable, oil-level/dipstick capture, separate dipstick reinsertion, cap/component touch sequence, engine-bay sweep, tire-pressure confirmation, oil-change-reminder reset. Never move dipstick capture after the touch sequence.
- Tire pressure is a technician statement, not an AI-verified measurement. AI observations are advisory, not verified service facts. Unresolved critical problems block approval.
- All employees may add notes; internal notes and customer-visible recommendations must remain separate.
- Future oil provider: AMSOIL. Future filter provider: ShowMeTheParts restricted to Service Champ. Never invent specifications or part numbers.
- Square remains the POS; integration is not implemented. Future customer report: simple page, one main video button, secure link, no customer account.
- Never claim camera, glasses, AI, storage, Square, or delivery are live when mocked. Explicit demo completion must remain separate from real inspection approval.
- Prefer existing dependencies: idb_shim for IndexedDB, package:web for browser bindings. The media assignment authorizes minimum maintained open-source capture/playback/storage dependencies; ask before unrelated additions. No global upgrades or deleting/resetting unrelated work. Commit/push only explicitly approved checkpoints.
- Current scope: real browser photo/video capture, binary storage and playback within the approved five-screen DEMO. Storage checkpoint 23a36ed is pushed; leave new media changes uncommitted. No SCR-006, video merging, live delivery or cloud integrations.
- Save session metadata and new media bytes atomically; await confirmed writes before Keep/approval and reject concurrent stale writes. Schema upgrades preserve existing records. Never reset damaged data, silently delete footage, or store only blob URLs. Preserve superseded files and original report references.
- Real captured media remains evidence of capture within a demo, not proof of safe service or AI interpretation. Label explicit simulations separately. Require explicit camera/microphone permission action, release resources on exit/background, validate finalized playable duration (five seconds for drain/filter), and preserve photo/reinsertion/caps order. Ask the user before physical-device testing.
- Before a physical test, identify the proposed camera and microphone, confirm Chrome's actual selections, and let the user prepare a non-sensitive scene with no private conversation. The user must approve the browser permission; never auto-grant device permissions or start capture from a test helper. Preview and recording remain separate actions. No background recording, upload or AI-provider transfer; release both devices at test end. Report computer-camera, synthetic and glasses testing separately.
- Stable test origin: http://127.0.0.1:8765/ in the same normal browser profile. Local storage is not backup/sync; clearing site data may remove it. Use fictional data only. Staff authentication, production encryption/key management, access controls and backup remain future work. Keep BUILD_STATUS current; format, analyze, test and build web.
